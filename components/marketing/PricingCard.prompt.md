One-line: pricing plan card — exactly one `featured` per row.

```jsx
<PricingCard name="Professional" featured price="£79"
  features={['Everything in Starter','Advanced analytics','Payroll tools']} />
<PricingCard name="Business" price="Custom" period="" ctaLabel="Contact Us" features={[…]} />
```

Prices are editable placeholders — store the three plans in one array and map.
