One-line: the Solutions-grid card — hover lifts it, borders it turquoise and nudges the arrow.

```jsx
<SolutionCard icon={<Icon name="Users" />} title="HR &amp; Workforce Management"
  description="Manage employees, attendance, leave, documents and workforce information from one platform." />
```

Use in a `repeat(3,1fr)` grid with 24px gap; `tone="dark"` in dark sections.
