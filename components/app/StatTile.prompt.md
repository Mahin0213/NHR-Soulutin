One-line: the KPI row at the top of every product screen (and the hero's floating cards).

```jsx
<StatTile label="Business Analytics" value="£182.4k" delta="+24.8%" caption="vs last month" icon={<Icon name="BarChart3" size={18} />} />
```

Positive deltas are turquoise, negative use `--nhr-danger`. Four across on desktop.
