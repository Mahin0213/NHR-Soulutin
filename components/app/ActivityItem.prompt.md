One-line: activity/notification row inside a `DashboardCard`.

```jsx
<ActivityItem icon={<Icon name="UserPlus" size={16} />} title="Leah Mensah added to Operations" meta="Employees" time="12m" />
```

Pass `divider={false}` on the last row.
