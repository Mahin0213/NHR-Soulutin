One-line: icon-only 40px control for chrome — mobile menu, close, notifications, search.

```jsx
<IconButton label="Open menu" tone="dark"><Menu size={20} /></IconButton>
```

Always pass `label`. Use `active` for the open/selected state.
