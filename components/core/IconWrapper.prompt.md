One-line: the 48px rounded square every card icon sits in.

```jsx
<IconWrapper><Users size={24} /></IconWrapper>
<IconWrapper tone="dark" size="lg"><ShieldCheck size={28} /></IconWrapper>
```

Lucide only, `currentColor`, 1.75 stroke. Never put an emoji inside.
