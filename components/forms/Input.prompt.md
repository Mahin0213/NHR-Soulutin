One-line: the single text-entry primitive — set `multiline` for a textarea.

```jsx
<Input label="Work email" type="email" placeholder="you@company.com" required />
<Input label="How can we help?" multiline rows={5} tone="dark" />
```

Always label. Helper text sits below in 13px muted; errors replace it.
